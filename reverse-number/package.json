{
  "name": "reverse-number",
  "version": "1.0.0",
  "description": "reverse a number without using string",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dilshika bandara",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
